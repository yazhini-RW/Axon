# virtualenv
.venv/

# Axon runtime data — the database, vector store and workflow checkpoints
data/

# real deliverables Axon builds for you — separate from Axon's own internal data
projects/
documents/

# a separate, persistent sandbox for demos — never mixes with real notes.
# demo/DEMO-SCRIPT.md itself stays tracked; only its data folders are ignored.
demo/data/
demo/projects/
demo/documents/

# secrets
.env

# python
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/

# editors / os
.vscode/
.idea/
Thumbs.db
desktop.ini
